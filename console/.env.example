VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_AUTH_DOMAIN=

# Set to "true" in .env.local to run against the Firebase Emulator Suite
# (no real Google account needed; use the Dev sign-in button).
VITE_USE_EMULATOR=false
